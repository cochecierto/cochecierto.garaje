# Nota de arquitectura

La ficha se implementará en el tema hijo y leerá datos de `garage_product` y sus metadatos. El plugin `CocheCierto Garage Core` mantiene el modelo y los permisos; el plugin Amazon solo aporta la oferta del proveedor.

